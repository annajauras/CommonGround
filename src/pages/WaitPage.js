const WaitPage = () => {
  return <h1>This is the waiting for next question page.</h1>;
};
export default WaitPage;

//Get in groups, press button that ready. If all players status = ready, show question and start timer
